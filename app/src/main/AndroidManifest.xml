<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.application.anicaremals">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera.any" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Anicaremals">
        <activity
            android:name=".ui.home.OnClickDetailActivity"
            android:exported="true" />
        <activity
            android:name=".ui.scanner.AddAnimalActivity"
            android:exported="true"></activity>
        <activity
            android:name=".ui.scanner.AnimalDetailsActivity"
            android:exported="true" /> <!-- <activity -->
        <!-- android:name=".ui.home.AnimalAddActivity" -->
        <!-- android:exported="true" /> -->
        android:theme="@style/Theme.Anicaremals" >
        <activity
            android:name=".ui.premium.ContentProviderMessageActivity"
            android:exported="true" >

        </activity>
        <activity
            android:name=".ui.premium.ContentProviderPhone_Activity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.home.MainActivity"
            android:exported="false" >
        </activity>
        <activity
            android:name=".ui.scanner.ScanAnimalActivity"
            android:exported="true"></activity>
    </application>

</manifest>